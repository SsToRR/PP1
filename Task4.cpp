#include <bits/stdc++.h>

using namespace std;

int main() {

    int a, b;

    cin >> a >> b;

    cout << (a >> b) << endl;

    //BITWISE shift bits of a by b positions to the right

    return 0;
}